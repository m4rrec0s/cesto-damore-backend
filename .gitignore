node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma
/tests
/dist

# Google Drive OAuth2 (NÃO COMMITAR!)
credentials.json
google-drive-credentials.json
google-drive-token.json

# Docker volumes - dados persistentes (NÃO VERSIONAR!)
code/
code/images/

# Pastas de dados locais (desenvolvimento e produção)
/images
/storage